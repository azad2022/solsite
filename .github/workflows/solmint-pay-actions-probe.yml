name: SolMint Pay Actions Probe

on:
  push:
    branches:
      - main
    paths:
      - .github/solmint-pay-actions-probe.trigger

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Probe
        run: echo "SolMint Pay Actions probe OK"
