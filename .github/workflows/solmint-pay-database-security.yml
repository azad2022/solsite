name: SolMint Pay Database Security

on:
  pull_request:
    paths:
      - "supabase/migrations/**"
      - "supabase/tests/**"
      - "functions/api/pay/**"
      - "src/pay/**"
      - ".github/workflows/solmint-pay-database-security.yml"
      - "docs/solmint-pay-live-migration-ledger.txt"
  push:
    branches: [main]
    paths:
      - "supabase/migrations/**"
      - "supabase/tests/**"
      - "functions/api/pay/**"
      - "src/pay/**"
      - ".github/workflows/solmint-pay-database-security.yml"
      - "docs/solmint-pay-live-migration-ledger.txt"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  database-security:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: solmint_pay_security
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d solmint_pay_security"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          persist-credentials: false

      - name: Validate repository migration chain
        run: bash supabase/tests/migration_chain_integrity.sh

      - name: Setup PostgreSQL client
        run: sudo apt-get update && sudo apt-get install -y postgresql-client

      - name: Run Pay identity/RLS security checks
        env:
          PGHOST: 127.0.0.1
          PGPORT: 5432
          PGUSER: postgres
          PGPASSWORD: postgres
          PGDATABASE: solmint_pay_security
        run: psql -v ON_ERROR_STOP=1 -f supabase/tests/pay_identity_rls_security.sql

      - name: Create isolated API-key security database
        env:
          PGHOST: 127.0.0.1
          PGPORT: 5432
          PGUSER: postgres
          PGPASSWORD: postgres
          PGDATABASE: postgres
        run: createdb pay_api_key_security

      - name: Create isolated report security database
        env:
          PGHOST: 127.0.0.1
          PGPORT: 5432
          PGUSER: postgres
          PGPASSWORD: postgres
          PGDATABASE: postgres
        run: createdb pay_report_security

      - name: Run Pay report revenue-ledger security checks
        env:
          PGHOST: 127.0.0.1
          PGPORT: 5432
          PGUSER: postgres
          PGPASSWORD: postgres
          PGDATABASE: pay_report_security
        run: psql -v ON_ERROR_STOP=1 -f supabase/tests/pay_report_revenue_ledger_security.sql

      - name: Run site security hardening checks
        env:
          PGHOST: 127.0.0.1
          PGPORT: 5432
          PGUSER: postgres
          PGPASSWORD: postgres
          PGDATABASE: solmint_pay_security
        run: psql -v ON_ERROR_STOP=1 -f supabase/tests/site_security_hardening.sql

      - name: Run Pay API-key lifecycle security checks
        env:
          PGHOST: 127.0.0.1
          PGPORT: 5432
          PGUSER: postgres
          PGPASSWORD: postgres
          PGDATABASE: pay_api_key_security
        run: psql -v ON_ERROR_STOP=1 -f supabase/tests/pay_api_key_lifecycle_security.sql
