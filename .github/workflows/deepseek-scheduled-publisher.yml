name: Solmint DeepSeek Scheduled Publisher

# Disabled intentionally. The DeepSeek chatbot remains available for manual
# use, but automatic article publishing is not currently used.
# Kept as a manually-dispatchable placeholder for future reactivation.

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: echo "DeepSeek scheduled publisher is intentionally disabled."