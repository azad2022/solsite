name: DeepSeek Auto Publish

# Disabled intentionally. The website uses the DeepSeek chatbot manually and
# does not currently use the automatic article publishing scheduler.
# Kept in the repository so it can be re-enabled later without reconstruction.

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: echo "DeepSeek auto-publish workflow is intentionally disabled."