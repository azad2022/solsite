name: SolMint Pay Devnet E2E

on:
  workflow_dispatch:
    inputs:
      run:
        description: 'Run the funded real-transaction Devnet E2E'
        required: true
        default: 'true'
        type: choice
        options:
          - 'true'
  push:
    branches: [main]
    paths:
      - 'src/pay/**'
      - 'functions/api/pay/**'
      - 'e2e/pay-devnet-native.e2e.ts'
      - 'package.json'
      - '.github/workflows/solmint-pay-devnet-e2e.yml'

permissions:
  contents: read

concurrency:
  group: pay-devnet-e2e-${{ github.ref }}
  cancel-in-progress: true

env:
  SOLANA_RPC_URL: ${{ secrets.SOLANA_DEVNET_RPC_URL || 'https://api.devnet.solana.com' }}

jobs:
  devnet-e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.2.15

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Require HTTPS Devnet RPC
        run: |
          case "$SOLANA_RPC_URL" in
            https://*) ;;
            *) echo "SOLANA_RPC_URL must use HTTPS"; exit 1 ;;
          esac

      - name: Run real Devnet payment verification E2E
        run: bun run test:pay:devnet
