name: SolMint Pay Devnet E2E

on:
  push:
    branches: [feat/solmint-pay-main-sync]
    paths:
      - 'src/pay/**'
      - 'tests/e2e/**'
      - 'tsconfig.e2e.json'
      - '.github/workflows/solmint-pay-devnet-e2e.yml'
  pull_request:
    branches: [main]
    paths:
      - 'src/pay/**'
      - 'tests/e2e/**'
      - 'tsconfig.e2e.json'
      - '.github/workflows/solmint-pay-devnet-e2e.yml'

permissions:
  contents: read

concurrency:
  group: solmint-pay-devnet-e2e-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  devnet-e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.2.15

      - name: Install repository dependencies
        run: npm install --no-audit --no-fund

      - name: Install test-only Solana harness dependency
        run: bun add --no-save @solana/web3.js@1.98.4

      - name: Typecheck Devnet E2E harness
        run: npx tsc -p tsconfig.e2e.json --noEmit

      - name: Verify funded Devnet signer is configured
        env:
          DEVNET_E2E_SENDER_SECRET_KEY: ${{ secrets.DEVNET_E2E_SENDER_SECRET_KEY }}
        run: |
          test -n "$DEVNET_E2E_SENDER_SECRET_KEY" || { echo 'DEVNET_E2E_SENDER_SECRET_KEY is required for Devnet E2E.' >&2; exit 1; }

      - name: Run real Devnet payment verification
        env:
          SOLANA_DEVNET_RPC_URL: https://api.devnet.solana.com
          DEVNET_E2E_SENDER_SECRET_KEY: ${{ secrets.DEVNET_E2E_SENDER_SECRET_KEY }}
        run: bun run tests/e2e/solmint-pay-devnet.e2e.ts
