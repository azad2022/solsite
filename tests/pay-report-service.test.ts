import assert from'node:assert/strict';import test from'node:test';import{createPayReportService}from'../src/pay/services/reportService';
const merchantId='22222222-2222-4222-8222-222222222222';
const report={periodStart:'2026-09-20T00:00:00.000Z',periodEnd:'2026-09-21T00:00:00.000Z',timezone:'UTC',paymentIntentCount:3,completedPaymentCount:1,pendingPaymentCount:1,exceptionPaymentCount:1,customerCount:2,completedPaymentAmountAtomic:'1000000',completedGatewayFeeSnapshotAtomic:'10000',grossGatewayFeeAtomic:'10000',referralCommissionAtomic:'1000',netGatewayRevenueAtomic:'9000',completedRateBps:3333,statusCounts:{completed:1,pending:1,failed:1},daily:[{date:'2026-09-20',paymentIntentCount:3,completedPaymentCount:1,completedPaymentAmountAtomic:'1000000'}]};
test('report service consumes authoritative report contract',async()=>{const calls:string[]=[];const service=createPayReportService({async request(path){calls.push(path);return{success:true,apiVersion:'v1',data:report}}});const result=await service.get(merchantId,'2026-09-20T00:00:00.000Z','2026-09-21T00:00:00.000Z');assert.equal(calls.length,1);assert.match(calls[0],/^\/api\/pay\/v1\/reports\?/);assert.equal(result.netGatewayRevenueAtomic,'9000');assert.equal(result.completedRateBps,3333);});
test('report service rejects invalid merchant id',async()=>{const service=createPayReportService({async request(){throw new Error('should not call')}});await assert.rejects(()=>service.get('invalid','a','b'),/Merchant ID is invalid/);});
